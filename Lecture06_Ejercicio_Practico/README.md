# Predicción de Ratings de Películas 🎬

Este proyecto aplica técnicas de **Aprendizaje Automático** para predecir la calificación (`RATING`) de películas. Utiliza un modelo de regresión basado en bosques aleatorios y un flujo de trabajo profesional con pipelines de preprocesamiento.

## 📋 Requisitos
Es necesario contar con Python 3.x y las siguientes librerías:
* `pandas`
* `numpy`
* `matplotlib`
* `scikit-learn`

Instalación rápida:
```bash
pip install pandas numpy matplotlib scikit-learn